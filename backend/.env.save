```

This will open the file in nano editor. Use your keyboard to navigate and type:
```
# Server Configuration
NODE_ENV=development
PORT=8080

# Google Cloud Project
GCP_PROJECT_ID=lokolo-platform
GCP_REGION=africa-south1

# Cloud SQL Connection
DB_HOST=localhost
DB_PORT=5433
DB_NAME=lokolo
DB_USER=postgres
DB_PASSWORD=EcUHu|?p23\S4"I`

# Firebase
FIREBASE_PROJECT_ID=lokolo-platform
FIREBASE_SERVICE_ACCOUNT_KEY_PATH=./firebase-service-account.json

# Cloud Storage
STORAGE_BUCKET=lokolo-platform.appspot.com

# API Configuration
API_VERSION=v1
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100# Server Configuration
NODE_ENV=development
PORT=8080

# Google Cloud Project
GCP_PROJECT_ID=lokolo-platform
GCP_REGION=africa-south1

# Cloud SQL Connectioncd ~/lokolo-platform/backend
nano .env
```

This will open the file in nano editor. Use your keyboard to navigate and type:
```
# Server Configuration
NODE_ENV=development
PORT=8080

# Google Cloud Project
GCP_PROJECT_ID=lokolo-platform
GCP_REGION=africa-south1

# Cloud SQL Connection
DB_HOST=localhost
DB_PORT=5433
DB_NAME=lokolo
DB_USER=postgres
DB_PASSWORD=EcUHu|?p23\S4"I`

# Firebase
FIREBASE_PROJECT_ID=lokolo-platform
FIREBASE_SERVICE_ACCOUNT_KEY_PATH=./firebase-service-account.json

# Cloud Storage
STORAGE_BUCKET=lokolo-platform.appspot.com

# API Configuration
API_VERSION=v1
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
DB_HOST=localhost
DB_PORT=5433
DB_NAME=lokolo
DB_USER=postgres
DB_PASSWORD=KU8?{)4C@rD}phX0

# Firebase
FIREBASE_PROJECT_ID=lokolo-platform
FIREBASE_SERVICE_ACCOUNT_KEY_PATH=./firebase-service-account.json

# Cloud Storage
STORAGE_BUCKET=lokolo-platform.appspot.com

# API Configuration
API_VERSION=v1
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
